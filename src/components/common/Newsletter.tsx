"use client";
import { useState } from "react";
export default function Newsletter(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const response=await fetch("/api/newsletter",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const result=await response.json() as {message:string};setMessage(result.message);if(response.ok)setEmail("");setBusy(false);}return <section className="bg-brand-cream px-5 py-12 text-center"><h2 className="font-display text-3xl">The QurZaib Edit</h2><p className="mt-2 text-sm text-muted">Receive new collection and fabric updates. No marketing cookies are required.</p><form onSubmit={submit} className="mx-auto mt-5 flex max-w-lg flex-col gap-2 sm:flex-row"><label className="sr-only" htmlFor="newsletter-email">Email address</label><input id="newsletter-email" required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="min-w-0 flex-1 border border-border bg-white px-4 py-3"/><button disabled={busy} className="bg-brand-green px-6 py-3 text-sm font-semibold text-white disabled:opacity-50">{busy?"Saving…":"Subscribe"}</button></form>{message&&<p aria-live="polite" className="mt-3 text-sm">{message}</p>}</section>}
