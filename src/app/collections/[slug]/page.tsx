import { notFound } from "next/navigation";import Link from "next/link";import { ProductCard } from "@/src/components/common/ProductCard";import { getCatalogPage,getCategoryBySlug } from "@/src/lib/catalog";
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const category=await getCategoryBySlug(slug);if(!category)notFound();const {products,total}=await getCatalogPage({q:"",category:category.id,productType:"",size:"",availability:"",sale:false,min:null,max:null,sort:"newest",page:1,pageSize:100});return <main className="mx-auto max-w-[1440px] px-5 py-14 sm:px-8 lg:px-12"><nav className="text-xs text-muted"><Link href="/">Home</Link> / <Link href="/collections">Collections</Link> / {category.name}</nav><h1 className="mt-5 font-display text-5xl">{category.name}</h1>{category.description&&<p className="mt-3 max-w-2xl text-muted">{category.description}</p>}<p className="mb-8 mt-2 text-sm text-muted">{total} products</p>{products.length?<div className="grid grid-cols-2 gap-x-3 gap-y-9 sm:gap-x-6 lg:grid-cols-4">{products.map(product=><ProductCard key={product.id} product={product}/>)}</div>:<p className="py-20 text-center text-muted">No active products in this category.</p>}</main>}
